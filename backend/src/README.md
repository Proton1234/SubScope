# RedditAnalytics.Api

This is the entry point for the ASP.NET Core Web API backend.

## Notes

- `Program.cs` is intentionally minimal for the skeleton.
- Add controllers and services under `src/Controllers` and `src/Services`.
- Configure EF Core and PostgreSQL in `src/Data`.
